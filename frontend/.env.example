# 前端服务配置
# 方式1: 本地开发时的配置
# VITE_BACKEND_HOST=127.0.0.1
# VITE_BACKEND_PORT=8000

# 方式2: 通过 Cloudflared 等暴露后，配置 Cloudflared 的 URL
# 例如: VITE_BACKEND_URL=https://your-subdomain.cloudflare.com
# 使用方式2时，方式1的配置会被忽略
VITE_BACKEND_URL=https://your-subdomain.cloudflare.com