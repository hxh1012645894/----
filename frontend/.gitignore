# Dependencies (can be rebuilt with npm install)
node_modules/

# Build output (can be rebuilt with npm run build)
dist/

# Environment files
.env
.env.local
.env.*.local

# IDE and editor
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Lock files debate
# Some teams choose to ignore package-lock.json
# We keep it for dependency consistency

# Test coverage (if added later)
coverage/